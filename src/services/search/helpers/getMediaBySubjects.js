/**
 * This would be just be a stub till we have the implementation of face tagging.
 * @param {*} subjects
 * @returns
 */
const getMediaBySubjects = (subjects) => {
    return [];
};

module.exports = {
    getMediaBySubjects,
};
